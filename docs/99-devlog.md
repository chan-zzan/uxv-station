# 개발 일지

> 그날 무슨 일이 있었고 뭐에 막혔는지를 남긴다.
> 나중에 케이스 스터디와 면접 답변의 원재료가 되는 기록.

## 작성 규칙

**분량: 한 항목당 5~7줄, 5분 안에.**
평일 저녁 1시간 작업인데 일지에 15분 쓰면 배보다 배꼽이다. 길게 쓰고 싶어지면 그건 ADR감이다.

- **`한 줄 요약`은 필수.** 진전이 없었던 날도 그대로 한 줄로 적는다.
- 나머지 항목은 각 1~2줄. 없으면 생략한다.
- 결정 내용을 길게 쓰지 않는다. `docs/decisions/`에 ADR로 남기고 여기선 링크만 건다.
  (일지가 ADR과 겹치기 시작하면 둘 다 부실해진다)

### 다른 기록 수단과의 역할 분담

| | 담는 것 |
|---|---|
| `git log` | 무엇이 바뀌었나 |
| `docs/decisions/` | **왜** 그렇게 결정했나 |
| **이 문서** | 그날 무슨 일이 있었나, 뭐에 막혔나 |

### ⚠️ 보안

**이 저장소는 공개다. 일지도 같이 공개된다.**
"회사에서 하던 방식대로 해보니…" 같은 문장이 일지에는 가장 자연스럽게 새어 나온다.
실무 프로젝트·실제 장비 명칭·군 제원·실 통신 규격을 언급하지 않는다.
([uxv-station-roadmap.md](uxv-station-roadmap.md) 3장)

### 서식

```markdown
## Day N — YYYY-MM-DD (요일) · 소요시간

**한 줄 요약**: (한 줄)

- **한 것** —
- **막힌 것** —
- **다음** —
```

---

## Day 1 — 2026-07-23 (목) · 약 1시간

**한 줄 요약**: 코드는 없음. 문서와 저장소 체계를 세웠다.

- **한 것** — `CLAUDE.md`를 주소록으로 설계(루트는 포인터, 내용은 `docs/`).
  로드맵을 주제별 문서로 분리하고 ADR 1호 작성. `.gitignore` → `git init` → GitHub public 저장소 push.
- **결정** — 경로 추종은 Pure Pursuit ([0001](decisions/0001-why-pure-pursuit.md)).
  커밋 이메일은 GitHub noreply — 실제 주소를 공개하지 않으면서 잔디는 찍히게.
- **배운 것** — 로드맵 8장의 "UE5 폴더 만들기 전에 `.gitignore`부터" 순서를 지켰다.
  반대였으면 수십 GB가 저장소에 들어간 뒤에 알아챘을 것.
- **다음** — Node.js LTS / Python 3.11+ 설치 확인 → 작업 순서 [1] 웹 UI + 스텁 퍼블리셔 착수
