# uxv-station

무인 차량 통합관제 데모. 시뮬레이터 + 통신 브리지 + 웹 운용통제기.
목적: 방산 경력직 이직 포트폴리오. **"설명할 수 있는가" > 완성도.**

현재 단계: **[1] 웹 UI + 스텁 퍼블리셔**

> 이 파일은 **주소록**이다. 내용을 담지 않고, 어디를 봐야 하는지만 가리킨다.
> 상세가 필요하면 해당 주소의 문서를 읽는다.

## 문서 주소록

| 알고 싶은 것 | 주소 |
|---|---|
| 프로젝트 전체 배경·결정·규칙 (기준 문서) | `docs/uxv-station-roadmap.md` |
| 메시지 계약 (VehicleState, SetWaypoints …) | `docs/00-message-contract.md` |
| 아키텍처·기술 스택 | `docs/01-architecture.md` |
| v2~v4 확장 아이디어 (구현 금지) | `docs/02-roadmap.md` |
| 왜 그렇게 결정했는가 (ADR) | `docs/decisions/` |
| 그날그날 뭘 했나 (개발 일지) | `docs/99-devlog.md` |

## 코드 주소록

| 덩어리 | 위치 | 기술 |
|---|---|---|
| 웹 운용통제 UI | `uxv-web/` | React + TS + Vite, `<canvas>` |
| 통신 브리지 | `uxv-bridge-server/` | Python + FastAPI (WebSocket) |
| 가짜 차량 퍼블리셔 | `uxv-stub/` | Python |
| 시뮬레이터 (나중 단계) | `uxv-sim/` | UE5, C++ 중심 |
| 데모 영상·스크린샷 | `media/` | (영상은 저장소에 넣지 않음) |

## 작업 규칙

1. **컨펌 우선** — 파일 생성·수정·삭제, 명령 실행 등 변경이 생기는 작업은
   먼저 계획을 설명하고 승인받은 뒤 진행한다. 읽기·탐색은 자유.
2. **추측 금지** — 불명확하면 임의로 가정하지 말고 질문한다.
3. **범위 고정** — v1 완료 기준(로드맵 4장) 초과 금지. 새 아이디어는 구현하지 말고
   `docs/02-roadmap.md` 아이디어 보관함에 적는다.
4. **귀속** — 핵심 알고리즘(짐벌 안정화, Pure Pursuit)은 원리 설명 →
   이해 확인 → 구현. 대신 써주지 말고 직접 쓸 수 있게 유도한다.
5. **결정 기록** — 결정이 생기면 `docs/decisions/`에 ADR을 남기도록 제안한다.
6. **보안** — 회사 자료·실제 장비 명칭·군 제원·실 IP 금지. 전부 일반 "무인 차량"으로 추상화.
7. **한 단계씩** — 로드맵 9장 작업 순서를 건너뛰지 않는다.
8. **일지** — 작업을 마무리하거나 사용자가 종료 의사를 밝히면, 끝내기 전에
   **`docs/99-devlog.md`에 오늘 기록을 남길지 항상 먼저 물어본다.**
   승인받으면 그때 추가한다. 묻지 않고 쓰지 않고, 물어보는 것 자체를 빠뜨리지 않는다.
   분량은 5~7줄. 길어질 내용은 일지가 아니라 ADR로 보낸다.
